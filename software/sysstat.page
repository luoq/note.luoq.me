[A useful webpage](http://www.thegeekstuff.com/2011/03/sar-examples/).

# collect memory usage

```
sar -r INTERVAL COUNT -o FILE

sar -r 1 3 -o memory.data
```

# convert data to other format

```
sadf DATA_FILE -p # for awk; default
sadf DATA_FILE -d # delimited
sadf DATA_FILE -x # xml
```